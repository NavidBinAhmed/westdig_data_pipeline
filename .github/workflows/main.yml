name: CI/CD Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: pip install -r requirements.txt

    #  - name: Run tests
    #    run: pytest tests/

      - name: Build Docker image
        run: docker build -t data-pipeline .

      - name: Deploy
        run: echo "Deploying application..."  # Add deployment script here
